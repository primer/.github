name: Release Event Tracking
# Measure a datadog event every time a release occurs

on:
  pull_request:
    types:
      - closed
      - opened
      - reopened

  release:
    types: [published]

jobs:
  release-tracking:
    name: Release Tracking
    uses: primer/.github/.github/workflows/release_tracking.yml@494b95a4368e635fef187fa727ad8d11a1934caf # main
    secrets:
      datadog_api_key: ${{ secrets.DATADOG_API_KEY }}
